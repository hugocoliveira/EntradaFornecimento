<?xml version="1.0" encoding="utf-8"?>
<!--
    Manifest exclusivo do build DEBUG.
    Adiciona intent-filter LAUNCHER para que o Android Studio consiga rodar o app diretamente.
    No build RELEASE este arquivo não existe, então o ícone permanece oculto no dispositivo.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity android:name=".MainActivity">
            <intent-filter>


            </intent-filter>
        </activity>
    </application>
</manifest>
